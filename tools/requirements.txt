# Only build_bathymetry.py needs these.
#
# make_sample_pmtiles.py, estimate_volume.py and the tests deliberately run on the
# standard library alone, so a contributor can generate a chart and run the pipeline
# tests without installing a geospatial stack.
rasterio>=1.3
numpy>=1.24
